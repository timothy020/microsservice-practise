server:
  port: 8083
hm:
  jwt:
    location: classpath:hmall.jks
    alias: hmall
    password: hmall123
    tokenTTL: 30m
  auth:
    excludePaths:
      - /search/**
      - /users/login
      - /items/**
      - /hi
  db:
    database: hm-user
  swagger:
    title: "黑马商城用户管理接口文档"
    package: com.hmall.user.controller
